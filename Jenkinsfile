pipeline {
  agent any
  stages {
    stage('pre-build') {
      steps {
        echo 'pre-build step 2025'
        sh 'whoami'
      }
    }

    stage('Build') {
      steps {
        echo 'bluid step'
      }
    }

    stage('deploy') {
      steps {
        echo 'deploy'
      }
    }

  }
}